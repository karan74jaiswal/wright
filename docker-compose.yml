services:
  redis:
    image: redis/redis-stack:latest
    container_name: wright_redis
    ports:
      - "127.0.0.1:6379:6379"
      - "127.0.0.1:8001:8001"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
